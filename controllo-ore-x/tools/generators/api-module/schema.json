{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "api-module",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "Library name",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    },
    "module": {
      "type": "string",
      "description": "Module base where to generate the files"
    }
  },
  "required": ["name"]
}
